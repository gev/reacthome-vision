import 'package:flutter/widgets.dart';

class DividerMacOS extends StatelessWidget {
  const DividerMacOS({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox();
  }
}
