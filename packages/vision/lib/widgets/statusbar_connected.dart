import 'package:flutter/material.dart';

class StatusbarConnected extends StatelessWidget {
  const StatusbarConnected({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("Connected"));
  }
}
