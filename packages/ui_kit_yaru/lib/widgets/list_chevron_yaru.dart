import 'package:flutter/material.dart';
import 'package:yaru/icons.dart';

class ListChevronYaru extends StatelessWidget {
  const ListChevronYaru({super.key});

  @override
  Widget build(BuildContext context) {
    return const Icon(YaruIcons.go_next);
  }
}
