(module screen.home
    (export home-screen)

    (def (home-screen args)
        (live-applet
            :id args.home.id
            :url args.home.url
            :title args.home.name
        )
    )
)
