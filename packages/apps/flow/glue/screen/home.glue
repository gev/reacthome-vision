(module screen.home
    (export home-screen)

    (def (home-screen args)
        (live-applet
            :id args.home.id
            :host args.home.host
            :title args.home.name
        )
    )
)
