import 'package:fluent_ui/fluent_ui.dart';

class ListChevronFluent extends StatelessWidget {
  const ListChevronFluent({super.key});

  @override
  Widget build(BuildContext context) {
    return const Icon(FluentIcons.chevron_right);
  }
}
